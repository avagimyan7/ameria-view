# Выгрузки банка — никогда не коммитим
*.xls
*.xlsx
*.csv
/data/
/statements/

# Личная часть настроек: номера счетов и ручные пометки, ключ которых — целая
# строка выписки. Держится только вне git (см. экран «Настройки»).
personal*.json

node_modules/
dist/
.DS_Store
/.design-baseline/
